[build-system]
requires = ["setuptools>=77.0.3", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nanopore3"
version = "0.3.0"
description = "PLACE-seq: Plate and Library Assignment, Consensus and Evaluation for Nanopore amplicons"
readme = "README.md"
requires-python = ">=3.10,<3.14"
authors = [
  { name = "Thomas Fryer", email = "tjaf@mit.edu" },
]
license = "MIT"
license-files = ["LICENSE"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Environment :: Console",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = [
  "edlib>=1.3.9.post1,<2",
  "PyYAML>=6,<7",
]

[project.optional-dependencies]
mcp = [
  "mcp>=1.28,<2",
  "psutil>=5.9,<8",
]
report = [
  "matplotlib>=3.8,<4",
  "numpy>=1.26,<3",
  "pandas>=2.2,<3",
  "scipy>=1.11,<2",
]
notebook = [
  "ipykernel>=6.29,<7",
  "jupyterlab>=4.2,<5",
]
dev = [
  "pytest>=8,<9",
  "pytest-cov>=5,<7",
  "ruff>=0.6,<1",
]

[project.scripts]
nanopore3 = "nanopore3.cli:main"
nanopore3-mcp = "nanopore3.mcp_server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
nanopore3 = ["example_data/*", "presets/*.yaml"]

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
